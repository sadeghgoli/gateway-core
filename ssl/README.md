# گواهی TLS

فایل `certificate.pfx` را اینجا بگذارید. رمز پیش‌فرض اسکریپت نصب `12345` است (با `PFX_PASSWORD` قابل تغییر).

اسکریپت آن را به `/etc/pki/nginx/fullchain.pem` و `privkey.pem` تبدیل می‌کند و برای **همه دامنه‌های گیت‌وی** و **پنل ادمین روی ۸۰۰۳** استفاده می‌شود.
